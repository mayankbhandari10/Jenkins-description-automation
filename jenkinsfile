pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo "Simulating Build Failure" && exit 1'
            }
        }
    }

    post {
        failure {
            script {
                currentBuild.description = "Build Failed: An error occurred during the build"
            }
        }
    }
}

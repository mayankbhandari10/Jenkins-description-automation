pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    try {
                        // Your build steps here
                    } catch (Exception e) {
                        // Capture the error message
                        currentBuild.description = "Build Failed: ${e.message}"
                        throw e
                    }
                }
            }
        }
    }
}

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    try {
                        sh dfs
                    } catch (Exception e) {
                        // Capture the error message
                        currentBuild.description = "Build Failed: ${e.message}"
                        throw e
                    }
                }
            }
        }
    }
}
